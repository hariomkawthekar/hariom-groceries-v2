services:
  - type: web
    name: hariom-groceries
    runtime: node
    region: oregon
    buildCommand: npm ci && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
